<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>בוטומט - בונים לך עסק חכם</title>
    <style>
        :root {
            --primary-color: #4CAF50;
            --secondary-color: #2196F3;
            --background-color: #f5f7fa;
            --text-color: #333;
        }
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, var(--background-color) 0%, #c3cfe2 100%);
            color: var(--text-color);
            line-height: 1.6;
        }
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            text-align: center;
        }
        .header {
            background: linear-gradient(45deg, var(--primary-color), var(--secondary-color));
            color: white;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 40px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .header h1 {
            margin: 0;
            font-size: 2.5em;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
        }
        .header p {
            font-size: 1.2em;
            margin-top: 10px;
        }
        .main-content {
            background-color: rgba(255, 255, 255, 0.8);
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            margin-bottom: 40px;
        }
        .btn {
            display: inline-block;
            background: linear-gradient(45deg, var(--primary-color), var(--secondary-color));
            color: white;
            padding: 15px 30px;
            text-decoration: none;
            border-radius: 50px;
            font-weight: bold;
            transition: all 0.3s ease;
            font-size: 1.1em;
            margin: 20px 0;
        }
        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
        }
        #iphone-demo {
            background: linear-gradient(45deg, #f3f4f6, #ddd);
            padding: 40px;
            border-radius: 10px;
            transition: all 0.5s ease;
        }
        .iphone-container {
            width: 300px;
            height: 600px;
            background-color: #fff;
            border-radius: 30px;
            margin: 0 auto;
            position: relative;
            overflow: hidden;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
        }
        .iphone-notch {
            width: 150px;
            height: 30px;
            background-color: #000;
            position: absolute;
            top: 0;
            left: 50%;
            transform: translateX(-50%);
            border-bottom-left-radius: 15px;
            border-bottom-right-radius: 15px;
            z-index: 1;
        }
        .whatsapp-header {
            background-color: #075E54;
            color: white;
            padding: 40px 10px 15px;
            text-align: center;
            font-weight: bold;
            position: relative;
            z-index: 0;
        }
        .whatsapp-screen {
            height: 450px;
            overflow-y: auto;
            padding: 10px;
            background-image: url('https://user-images.githubusercontent.com/15075759/28719144-86dc0f70-73b1-11e7-911d-60d70fcded21.png');
            background-size: cover;
        }
        .message {
            max-width: 80%;
            margin-bottom: 10px;
            padding: 10px;
            border-radius: 7.5px;
            position: relative;
            clear: both;
            font-size: 0.9em;
            box-shadow: 0 1px 0.5px rgba(0,0,0,0.13);
        }
        .message::after {
            content: attr(data-time);
            position: absolute;
            bottom: 3px;
            right: 7px;
            font-size: 0.75em;
            color: #999;
        }
        .bot-message {
            background-color: #ffffff;
            float: left;
            border-top-left-radius: 0;
            text-align: right;
        }
        .user-message {
            background-color: #dcf8c6;
            float: right;
            border-top-right-radius: 0;
        }
        .whatsapp-input {
            display: flex;
            padding: 15px 10px;
            background-color: #f0f0f0;
            align-items: center;
            position: absolute;
            bottom: 0;
            width: 100%;
            box-sizing: border-box;
        }
        .whatsapp-input input {
            flex-grow: 1;
            border: none;
            border-radius: 20px;
            padding: 10px 15px;
            margin-right: 10px;
            font-size: 16px;
        }
        .whatsapp-input button {
            background-color: #075E54;
            color: white;
            border: none;
            border-radius: 50%;
            width: 40px;
            height: 40px;
            font-size: 18px;
            cursor: pointer;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        @media (max-width: 768px) {
            .iphone-container {
                width: 100%;
                max-width: 300px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header class="header">
            <h1>בוטומט</h1>
            <p>בונים לך עסק חכם עם טכנולוגיה מתקדמת</p>
        </header>

        <div class="main-content">
            <h2>ברוכים הבאים לבוטומט!</h2>
            <p>אנחנו כאן כדי לעזור לך לבנות עסק חכם ומצליח. עם הכלים המתקדמים שלנו,
            תוכל להגדיל את ההכנסות, לייעל תהליכים ולהגיע ללקוחות חדשים.</p>
            <a href="#iphone-demo" class="btn">נסה את הבוט שלנו</a>
        </div>

        <div id="iphone-demo">
            <h2>הדגמת הבוט שלנו</h2>
            <div class="iphone-container">
                <div class="iphone-notch"></div>
                <div class="whatsapp-header">
                    בוטומט ∙ בוטים חכמים לעסק
                </div>
                <div id="chat-screen" class="whatsapp-screen">
                    <div class="message bot-message" data-time="10:03">
                        שלום! אני הבוט של בוטומט. איך אוכל לעזור לך היום? האם תרצה מידע על:
                        1. הגדלת מכירות
                        2. ייעול תהליכים
                        3. שיווק דיגיטלי
                    </div>
                </div>
                <div class="whatsapp-input">
                    <input type="text" placeholder="הקלד הודעה..." id="user-input">
                    <button onclick="sendMessage()">
                        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                            <path d="M2.01 21L23 12 2.01 3 2 10l15 2-15 2z" fill="white"/>
                        </svg>
                    </button>
                </div>
            </div>
        </div>

        <a href="https://wa.me/972554409246?text=אשמח+לקבל+פרטים+על+בוטים+בווצאפ" class="btn" target="_blank">צור קשר בווצאפ</a>
    </div>

    <script>
        function sendMessage() {
            const input = document.getElementById('user-input');
            const message = input.value.trim();
            if (message) {
                addMessage(message, 'user-message');
                input.value = '';
                setTimeout(() => respondToUser(message), 1000);
            }
        }

        function addMessage(text, className) {
            const chatScreen = document.getElementById('chat-screen');
            const messageDiv = document.createElement('div');
            messageDiv.className = `message ${className}`;
            messageDiv.textContent = text;
            const now = new Date();
            messageDiv.setAttribute('data-time', `${now.getHours()}:${now.getMinutes().toString().padStart(2, '0')}`);
            chatScreen.appendChild(messageDiv);
            chatScreen.scrollTop = chatScreen.scrollHeight;
        }

        function respondToUser(userMessage) {
            let response;
            if (userMessage.includes('1') || userMessage.toLowerCase().includes('מכירות')) {
                response = 'מצוין! להגדלת מכירות, אנו מציעים כלים לניתוח נתונים ואוטומציה של תהליכי מכירה. האם תרצה לשמוע עוד על כך?';
            } else if (userMessage.includes('2') || userMessage.toLowerCase().includes('תהליכים')) {
                response = 'לייעול תהליכים, אנו מספקים פתרונות אוטומציה מתקדמים. האם יש תהליך ספציפי שתרצה לייעל?';
            } else if (userMessage.includes('3') || userMessage.toLowerCase().includes('שיווק')) {
                response = 'בתחום השיווק הדיגיטלי, אנו מציעים כלים לניהול קמפיינים ואנליטיקה. האם תרצה לשמוע על הצלחות של לקוחות שלנו?';
            } else {
                response = 'תודה על ההודעה. האם תוכל לספק מידע נוסף על מה שאתה מחפש? אשמח לעזור בכל נושא הקשור להגדלת מכירות, ייעול תהליכים או שיווק דיגיטלי.';
            }
            addMessage(response, 'bot-message');
        }

        document.getElementById('user-input').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                sendMessage();
            }
        });

        document.querySelector('.btn').addEventListener('click', function(e) {
            e.preventDefault();
            document.getElementById('iphone-demo').scrollIntoView({ behavior: 'smooth' });
        });
    </script>
</body>
</html>
